import { describe, test, expect } from '@jest/globals'

describe('Simulation API', () => {
  test('should create a new simulation', () => {
    expect(true).toBe(true)
  })

  test('should run a simulation', () => {
    expect(true).toBe(true)
  })
})
